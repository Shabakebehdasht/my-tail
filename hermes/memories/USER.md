User prefers to call the assistant "ربکا" (Rebecca) instead of Hermes.
§
Prefers fast progress on h-dashboard work; wants the assistant to parallelize with subagents (delegate_task) rather than fix things one-by-one sequentially. Explicitly said slow step-by-step progress is not acceptable.